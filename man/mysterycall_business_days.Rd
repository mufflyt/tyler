% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/business_days.R
\name{mysterycall_business_days}
\alias{mysterycall_business_days}
\title{Business-day utilities for mystery caller studies}
\usage{
mysterycall_business_days(
  data,
  call_col = "call_date",
  appt_col = "appointment_date",
  result_col = "business_days_until_appointment",
  calendar = NULL
)
}
\arguments{
\item{data}{A data frame containing date columns for the call and the
offered appointment.}

\item{call_col}{Name of the column holding the call date. Defaults to
\code{"call_date"}.}

\item{appt_col}{Name of the column holding the appointment date. Defaults
to \code{"appointment_date"}.}

\item{result_col}{Name of the new column to create. Defaults to
\code{"business_days_until_appointment"}, which matches the outcome name used
throughout the package.}

\item{calendar}{A \code{bizdays} calendar from \code{\link[=mysterycall_us_federal_calendar]{mysterycall_us_federal_calendar()}}.
Built automatically when \code{NULL} (default). Pass a pre-built calendar when
calling this function many times to avoid rebuilding it on each call.}
}
\value{
\code{data} with one additional integer column named \code{result_col}.
}
\description{
The primary outcome in mystery caller studies is
\code{business_days_until_appointment}: the number of Mon-Fri working days
(excluding US federal holidays) between the call date and the offered
appointment date. These helpers build a \code{bizdays} calendar from US
federal holidays and compute that column for a data frame.

Computes \code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}} for every row and stores the
result in a new column. This is the primary way to produce the
\code{business_days_until_appointment} outcome column needed for
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}.
}
\examples{
df <- data.frame(
  physician        = c("Dr. Smith", "Dr. Jones"),
  call_date        = as.Date(c("2026-02-02", "2026-02-13")),
  appointment_date = as.Date(c("2026-02-10", "2026-02-20"))
)
if (requireNamespace("bizdays", quietly = TRUE)) {
  mysterycall_business_days(df)
}
}
\seealso{
\code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}}, \code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}

Other business days: 
\code{\link{mysterycall_count_business_days}()},
\code{\link{mysterycall_us_federal_calendar}()}
}
\concept{business days}
