% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_disparities.R
\name{mysterycall_plot_disparities}
\alias{mysterycall_plot_disparities}
\title{Plot disparity metrics from a \code{mysterycall_disparities_table}}
\usage{
mysterycall_plot_disparities(
  x,
  metric = c("rate", "abs_diff", "rel_risk"),
  show_ref = TRUE,
  show_p = TRUE,
  color_sig = "#C0392B",
  color_ns = "#2C3E50",
  color_ref = "#2166AC",
  point_size = 3,
  x_label = NULL,
  x_pct = NULL,
  title = NULL
)
}
\arguments{
\item{x}{A \code{mysterycall_disparities_table} object returned by
\code{\link[=mysterycall_disparities_table]{mysterycall_disparities_table()}}, \strong{or} a plain data frame with at least
the columns required by the chosen \code{metric} (see Details).}

\item{metric}{Character scalar. Which disparity metric to display on the
x-axis:
\describe{
\item{\code{"rate"} (default)}{Appointment-offered rate for each group with
its CI. A vertical dashed reference line marks the reference group
rate.}
\item{\code{"abs_diff"}}{Absolute difference in percentage points versus the
reference group. Reference line at 0.}
\item{\code{"rel_risk"}}{Relative risk versus the reference group. Reference
line at 1.}
}}

\item{show_ref}{Logical. When \code{TRUE} (default) the reference group row is
included in the plot, drawn with \code{color_ref} and a diamond shape.}

\item{show_p}{Logical. When \code{TRUE} (default) the formatted p-value is
annotated to the right of each non-reference point.}

\item{color_sig}{Character. Colour for groups whose \code{p_value < 0.05}.
Default \code{"#C0392B"} (red).}

\item{color_ns}{Character. Colour for non-significant groups (or when
\code{p_value} is absent). Default \code{"#2C3E50"} (dark navy).}

\item{color_ref}{Character. Colour for the reference group point.
Default \code{"#2166AC"} (blue).}

\item{point_size}{Numeric. Diameter of the estimate point. Default \code{3}.}

\item{x_label}{Character or \code{NULL}. X-axis label. \code{NULL} (default) picks a
label automatically from \code{metric}.}

\item{x_pct}{Logical. When \code{TRUE} (default for \code{"rate"} and \code{"abs_diff"})
the x-axis is formatted as a percentage. Ignored when \code{metric = "rel_risk"}.}

\item{title}{Character or \code{NULL}. Plot title. Default \code{NULL} (no title).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot}} object. Print it to display or pass to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} to save.
}
\description{
Produces a publication-ready dot plot of acceptance rates, absolute
differences, or relative risks computed by \code{\link[=mysterycall_disparities_table]{mysterycall_disparities_table()}}.
The function is the binary-outcome analogue of \code{\link[=mysterycall_irr_plot]{mysterycall_irr_plot()}}:
call \code{mysterycall_disparities_table()} first, then pipe the result here.
}
\details{
\strong{Required columns by metric:}
\describe{
\item{\code{"rate"}}{\code{group}, \code{rate}, \code{lower_ci}, \code{upper_ci}}
\item{\code{"abs_diff"}}{\code{group}, \code{abs_diff}, \code{lower_ci}, \code{upper_ci}
(CIs are re-centred automatically)}
\item{\code{"rel_risk"}}{\code{group}, \code{rel_risk}, \code{rr_lower}, \code{rr_upper}}
}
When \code{x} is a \code{mysterycall_disparities_table} object the reference group
and significance attributes are read directly from the object's attributes.
When \code{x} is a plain data frame, a \code{p_value} column is used for significance
colouring if present, and the first row is treated as the reference group
when \code{show_ref = TRUE}.
}
\examples{
set.seed(42)
df <- data.frame(
  insurance = sample(c("Medicaid", "Medicare", "Private", "Uninsured"),
                     300, replace = TRUE),
  accepted  = rbinom(300, 1, prob = rep(c(0.64, 0.84, 0.91, 0.54), 75))
)
tbl <- mysterycall_disparities_table(df, "accepted", "insurance",
                                      ref_group = "Private")
mysterycall_plot_disparities(tbl)
mysterycall_plot_disparities(tbl, metric = "abs_diff")
mysterycall_plot_disparities(tbl, metric = "rel_risk")
}
\seealso{
\code{\link[=mysterycall_disparities_table]{mysterycall_disparities_table()}}, \code{\link[=mysterycall_irr_plot]{mysterycall_irr_plot()}}

Other plotting: 
\code{\link{mysterycall_plot_emmeans}()},
\code{\link{mysterycall_plot_source_venn}()}
}
\concept{plotting}
