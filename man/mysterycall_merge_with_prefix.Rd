% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_utils.R
\name{mysterycall_merge_with_prefix}
\alias{mysterycall_merge_with_prefix}
\title{Merge two data frames with source-prefixed column names}
\usage{
mysterycall_merge_with_prefix(
  x,
  y,
  by,
  prefix_x = "x_",
  prefix_y = "y_",
  join_type = c("left", "inner", "full", "right")
)
}
\arguments{
\item{x}{A data frame (left table).}

\item{y}{A data frame (right table).}

\item{by}{Character vector of column names to join on. These columns are
NOT prefixed.}

\item{prefix_x}{Character scalar prefix applied to non-key columns of \code{x}.
Default \code{"x_"}.}

\item{prefix_y}{Character scalar prefix applied to non-key columns of \code{y}.
Default \code{"y_"}.}

\item{join_type}{One of \code{"left"} (default), \code{"inner"}, \code{"full"}, or
\code{"right"}.}
}
\value{
A data frame in this column order: (1) join key columns (names
unchanged), (2) non-key columns of \code{x} with \code{prefix_x} prepended (e.g.
\code{"specialty"} becomes \code{"npi_specialty"}), (3) non-key columns of \code{y} with
\code{prefix_y} prepended. Row order follows base R \code{merge()} (sorted by key).
For \code{join_type = "left"}, unmatched \code{y} columns are \code{NA}.
}
\description{
Renames all non-key columns in \code{x} with \code{prefix_x} and all non-key
columns in \code{y} with \code{prefix_y}, then performs a left join on \code{by}.
This keeps columns traceable (e.g. \code{findent_state} vs \code{abohns_state})
when two data sources are combined.
}
\examples{
registry <- data.frame(npi = c("A","B"), state = c("CO","TX"),
                       specialty = c("ENT","ENT"), stringsAsFactors = FALSE)
abohns    <- data.frame(npi = c("A","C"), cert = c("Neurotology",NA),
                        stringsAsFactors = FALSE)
mysterycall_merge_with_prefix(registry, abohns, by = "npi",
                               prefix_x = "npi_", prefix_y = "abohns_")
}
\seealso{
\code{\link[=mysterycall_safe_left_join]{mysterycall_safe_left_join()}} for joins with duplicate-key
warnings; \code{\link[=mysterycall_reconcile_specialty]{mysterycall_reconcile_specialty()}} for post-join specialty
harmonisation.

Other data management: 
\code{\link{mysterycall_check_duplicates}()},
\code{\link{mysterycall_check_generalist_presence}()},
\code{\link{mysterycall_extract_zip5}()},
\code{\link{mysterycall_luhn_check}()},
\code{\link{mysterycall_prepare_table1_vars}()},
\code{\link{mysterycall_rename_columns}()},
\code{\link{mysterycall_stratified_sample}()}
}
\concept{data management}
