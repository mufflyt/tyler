% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_table.R
\name{mysterycall_model_table}
\alias{mysterycall_model_table}
\title{Word-ready IRR table from a fitted Poisson model}
\usage{
mysterycall_model_table(
  x,
  include_intercept = FALSE,
  digits = 2L,
  irr_col = "IRR (95\% CI)",
  p_col = "p-value",
  term_col = "Term"
)
}
\arguments{
\item{x}{A \code{mysterycall_poisson_model} object from
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}.}

\item{include_intercept}{Logical. When \code{FALSE} (default) the \code{(Intercept)}
row is dropped.}

\item{digits}{Integer. Decimal places for IRR and CI values. Default \code{2}.}

\item{irr_col}{Character. Name of the combined "IRR (95\% CI)" column.
Default \code{"IRR (95\% CI)"}.}

\item{p_col}{Character. Name of the p-value column. Default \code{"p-value"}.}

\item{term_col}{Character. Name of the term column. Default \code{"Term"}.}
}
\value{
A data frame (tibble) with columns \code{term_col}, \code{irr_col}, and
\code{p_col}. One row per fixed-effect term.
}
\description{
Extracts and formats the fixed-effects table from a
\code{mysterycall_poisson_model} result into a print-ready data frame. Column
names and values follow standard epidemiological reporting conventions
(IRR with 95\% CI in a single "IRR (95\% CI)" column, formatted p-values).
The returned data frame can be passed directly to \code{flextable::flextable()}
or \code{knitr::kable()} for Word/HTML output.
}
\examples{
if (requireNamespace("lme4", quietly = TRUE)) {
  set.seed(1978)
  df <- data.frame(
    wait_days = rpois(40, lambda = 18),
    insurance = rep(c("Medicaid", "BCBS"), each = 20),
    physician = rep(paste0("Dr_", 1:8), each = 5),
    stringsAsFactors = FALSE
  )
  result <- suppressWarnings(mysterycall_poisson_model(
    df, "wait_days", "insurance", "physician"
  ))
  mysterycall_model_table(result)
}
}
\seealso{
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}, \code{\link[=mysterycall_irr_plot]{mysterycall_irr_plot()}}

Other table helpers: 
\code{\link{mysterycall_disparities_table}()},
\code{\link{mysterycall_format_pct}()},
\code{\link{mysterycall_max_table}()},
\code{\link{mysterycall_min_table}()},
\code{\link{mysterycall_table_percentages}()},
\code{\link{mysterycall_table_proportion}()},
\code{\link{print.mysterycall_disparities_table}()}
}
\concept{table helpers}
