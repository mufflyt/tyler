% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_calculate_percentages.R
\name{mysterycall_table_percentages}
\alias{mysterycall_table_percentages}
\title{Most frequent level(s) of a categorical variable with percentage}
\usage{
mysterycall_table_percentages(data_frame, variable)
}
\arguments{
\item{data_frame}{A data frame containing the categorical variable.}

\item{variable}{A character string giving the column name of the categorical
variable within \code{data_frame}.}
}
\value{
A data frame with one row per level tied for the highest count.
Columns: the variable itself, \code{n} (count), and \code{percent} (exact percentage
of non-missing rows, not rounded).
}
\description{
Counts each level of a categorical variable, computes its share of all
rows (including \code{NA}), and returns \strong{only the level(s) with the highest count}.
When multiple levels tie for the top count, all tied levels are returned.
\code{NA} values are counted as their own level and included in the denominator.
}
\examples{
# "A" is most frequent: returns 1 row
data_frame <- data.frame(category = c("A", "B", "A", "C", "A", "B", "B", "A"))
mysterycall_table_percentages(data_frame, "category")

# Three-way tie: all three returned
df_tie <- data.frame(category = c("A", "B", "A", "B", "C", "C", "C", "A", "B"))
mysterycall_table_percentages(df_tie, "category")

# NAs are counted as their own level; denominator includes all rows
df_na <- data.frame(category = c("A", NA, "A", "C", "A", "B", "B", NA))
mysterycall_table_percentages(df_na, "category")

}
\seealso{
\code{\link[=mysterycall_table_proportion]{mysterycall_table_proportion()}}, \code{\link[=mysterycall_most_common_gender]{mysterycall_most_common_gender()}},
\code{\link[=mysterycall_min_table]{mysterycall_min_table()}}, \code{\link[=mysterycall_max_table]{mysterycall_max_table()}}

Other table helpers: 
\code{\link{mysterycall_disparities_table}()},
\code{\link{mysterycall_format_pct}()},
\code{\link{mysterycall_max_table}()},
\code{\link{mysterycall_min_table}()},
\code{\link{mysterycall_model_table}()},
\code{\link{mysterycall_table_proportion}()},
\code{\link{print.mysterycall_disparities_table}()}
}
\concept{table helpers}
