% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poisson_model.R
\name{mysterycall_poisson_model}
\alias{mysterycall_poisson_model}
\title{Fit a Poisson GLMER for mystery caller wait-time analysis}
\usage{
mysterycall_poisson_model(
  data,
  outcome,
  predictors,
  random_intercept,
  conf_level = 0.95,
  nAGQ = 0L,
  offset_col = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing all model columns. Rows with \code{NA} in
any model column are dropped before fitting; the count is reported.}

\item{outcome}{Character scalar naming the count-outcome column (e.g.
\code{"business_days_until_appointment"}). Must be non-negative numeric.}

\item{predictors}{Character vector of fixed-effect predictor column names.
Factor and character columns are used as-is; their reference level is
the first level alphabetically (or the first \code{levels()} for factors).}

\item{random_intercept}{Character scalar naming the grouping column for
the random intercept (e.g. \code{"name"} for physician). A \code{(1 | column)}
term is added to the formula automatically.}

\item{conf_level}{Confidence level for Wald CIs. Default \code{0.95}.}

\item{nAGQ}{Integer passed to \code{\link[lme4:glmer]{lme4::glmer()}}. \code{0} (default) uses the
fastest approximation; \code{1} uses the Laplace approximation; values \verb{> 1}
use adaptive Gauss-Hermite quadrature (slower, more accurate for small
cluster sizes).}

\item{offset_col}{Optional character scalar naming a numeric column to use
as a log-offset (e.g. log(exposure time)). When supplied, the term
\code{offset(log(offset_col))} is appended to the fixed-effects formula.}

\item{...}{Additional arguments forwarded to \code{\link[lme4:glmer]{lme4::glmer()}}.}
}
\value{
A list of class \code{mysterycall_poisson_model} containing:
\describe{
\item{\code{model}}{The fitted \code{glmerMod} object.}
\item{\code{irr_table}}{Tibble with one row per fixed-effect term:
\code{term}, \code{estimate} (log scale), \code{se}, \code{z_value}, \code{p_value},
\code{p_value_fmt} (character), \code{irr}, \code{ci_lower}, \code{ci_upper}.}
\item{\code{random_effects}}{Data frame from \code{\link[lme4:VarCorr]{lme4::VarCorr()}} describing
the random-intercept variance and standard deviation.}
\item{\code{factor_refs}}{Named list of reference levels for character/factor
predictors.}
\item{\code{formula}}{The formula passed to \code{glmer}.}
\item{\code{n}}{Number of complete-case rows used for fitting.}
\item{\code{n_dropped}}{Rows excluded due to missing values.}
\item{\code{n_clusters}}{Number of unique values of \code{random_intercept}.}
\item{\code{overdispersion}}{Pearson chi-square / residual df. Values
substantially above 1 suggest overdispersion; consider a negative
binomial model.}
\item{\code{convergence}}{List with \code{converged} (logical), \code{singular}
(logical), and \code{messages} (character).}
\item{\code{aic}, \code{bic}}{Model information criteria.}
}
}
\description{
Runs a multilevel Poisson regression (\code{lme4::glmer}) appropriate for
mystery caller studies where the same physician may be called multiple
times (e.g. with different insurance types). The physician identifier is
modelled as a random intercept, which accounts for correlation within
physicians. Fixed-effect results are returned as incidence rate ratios
(IRR) with Wald confidence intervals.
}
\section{Interpreting IRRs}{

An IRR of 1.40 for \code{insurance_typeMedicaid} means physicians contacted
with Medicaid insurance had, on average, 40\% longer wait times than the
reference insurance group. Compute as \code{exp(estimate)} with Wald CI
\verb{exp(estimate ± z * se)}.
}

\section{Overdispersion}{

Poisson assumes mean = variance. If \code{overdispersion} is substantially
greater than 1 (a common threshold is 2), the standard errors will be
underestimated. Consider fitting a negative binomial model or using
quasi-Poisson standard errors for inference.
}

\examples{
\dontshow{if (requireNamespace("lme4", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1978)
df <- data.frame(
  wait_days = rpois(40, lambda = 18),
  insurance = rep(c("Medicaid", "BCBS"), each = 20),
  gender    = sample(c("Male", "Female"), 40, replace = TRUE),
  physician = rep(paste0("Dr_", 1:8), each = 5),
  stringsAsFactors = FALSE
)
result <- mysterycall_poisson_model(
  df,
  outcome          = "wait_days",
  predictors       = c("insurance", "gender"),
  random_intercept = "physician"
)
result$irr_table
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_wait_time_summary]{mysterycall_wait_time_summary()}}, \code{\link[=mysterycall_table1]{mysterycall_table1()}},
\code{\link[=mysterycall_create_formula]{mysterycall_create_formula()}}

Other outcomes: 
\code{\link{mysterycall_acceptance_rate}()},
\code{\link{mysterycall_irr_plot}()},
\code{\link{mysterycall_marginal_effects}()},
\code{\link{mysterycall_model_metrics}()},
\code{\link{mysterycall_model_table}()},
\code{\link{mysterycall_plot_distribution}()},
\code{\link{mysterycall_plot_effect}()},
\code{\link{mysterycall_plot_emmeans_full}()},
\code{\link{mysterycall_plot_emmeans_interaction}()},
\code{\link{mysterycall_plot_inclexcl}()},
\code{\link{mysterycall_plot_residuals}()},
\code{\link{mysterycall_plot_sjplot_interaction}()},
\code{\link{mysterycall_plot_stacked_bar}()},
\code{\link{mysterycall_save_plot}()},
\code{\link{mysterycall_screen_interactions}()},
\code{\link{mysterycall_select_best_model}()},
\code{\link{mysterycall_table1}()},
\code{\link{mysterycall_wait_time_summary}()}
}
\concept{outcomes}
