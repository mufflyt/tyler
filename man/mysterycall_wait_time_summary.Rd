% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome_analysis.R
\name{mysterycall_wait_time_summary}
\alias{mysterycall_wait_time_summary}
\title{Summarise appointment wait times}
\usage{
mysterycall_wait_time_summary(
  data,
  wait_col = "wait_days",
  group_by = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame containing at least the column named by \code{wait_col}.}

\item{wait_col}{Name of the numeric column holding days to appointment.
Defaults to \code{"wait_days"}.}

\item{group_by}{Optional character scalar naming a grouping column (e.g.
\code{"insurance"}). When \code{NULL} (default) statistics are computed over the
full dataset and no test is run.}

\item{conf_level}{Confidence level used by the Wilcoxon test. Default
\code{0.95}.}
}
\value{
A named list with:
\describe{
\item{\code{summary}}{Tibble with one row per group (or one row overall).
When \code{group_by} is non-\code{NULL}, the first column is named after the
grouping variable (character). Remaining columns: \code{n} (integer),
\code{n_missing} (integer), \code{mean} (numeric), \code{sd} (numeric), \code{median}
(numeric), \code{q1} (numeric), \code{q3} (numeric), \code{min} (numeric), \code{max}
(numeric).}
\item{\code{test}}{\code{htest} object from \code{\link[stats:wilcox.test]{stats::wilcox.test()}} (two groups)
or \code{\link[stats:kruskal.test]{stats::kruskal.test()}} (three or more groups), or \code{NULL} when no
test was run (i.e., \code{group_by = NULL}).}
\item{\code{p_value}}{Numeric p-value extracted from \code{test}, or \code{NA_real_}
when no test was run.}
\item{\code{test_name}}{Character. One of \code{"Wilcoxon rank-sum"},
\code{"Kruskal-Wallis"}, or \code{"none"}.}
\item{\code{interpretation}}{Character. One-sentence plain-language summary
suitable for pasting into a Results section.}
}
}
\description{
Computes descriptive statistics (n, mean, SD, median, IQR, range) for a
numeric wait-time column and runs an appropriate non-parametric test when
groups are specified: Wilcoxon rank-sum for two groups, Kruskal-Wallis for
three or more.
}
\details{
Wait times must already be numeric (days to appointment). If your Phase 2
data records an appointment date rather than a count, compute
\code{data$wait_days <- as.numeric(data$appt_date - data$call_date)} before
calling this function.
}
\examples{
df <- data.frame(
  wait_days = c(3, 7, 14, 21, 5, 10, 30, 2),
  insurance = c("Medicaid", "Medicaid", "Medicaid", "Medicaid",
                "BCBS", "BCBS", "BCBS", "BCBS")
)
mysterycall_wait_time_summary(df, group_by = "insurance")
}
\seealso{
\code{\link[=mysterycall_acceptance_rate]{mysterycall_acceptance_rate()}}, \code{\link[=mysterycall_clean_phase2]{mysterycall_clean_phase2()}}

Other outcomes: 
\code{\link{mysterycall_acceptance_rate}()},
\code{\link{mysterycall_irr_plot}()},
\code{\link{mysterycall_marginal_effects}()},
\code{\link{mysterycall_model_metrics}()},
\code{\link{mysterycall_plot_distribution}()},
\code{\link{mysterycall_plot_effect}()},
\code{\link{mysterycall_plot_emmeans_full}()},
\code{\link{mysterycall_plot_emmeans_interaction}()},
\code{\link{mysterycall_plot_inclexcl}()},
\code{\link{mysterycall_plot_residuals}()},
\code{\link{mysterycall_plot_sjplot_interaction}()},
\code{\link{mysterycall_plot_stacked_bar}()},
\code{\link{mysterycall_poisson_model}()},
\code{\link{mysterycall_screen_interactions}()},
\code{\link{mysterycall_select_best_model}()},
\code{\link{print.mysterycall_poisson_model}()}
}
\concept{outcomes}
