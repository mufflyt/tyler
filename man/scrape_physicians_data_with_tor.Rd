% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_physicians.R
\name{scrape_physicians_data_with_tor}
\alias{scrape_physicians_data_with_tor}
\title{Scrape Physicians' Data with Tor}
\usage{
scrape_physicians_data_with_tor(
  startID,
  endID,
  torPort,
  wrong_ids_path = NULL,
  timeout_sec = 30,
  delay_sec = 1,
  output_dir = tempdir(),
  output_format = c("csv", "parquet")
)
}
\arguments{
\item{startID}{The starting diplomate ID for scraping.}

\item{endID}{The ending diplomate ID for scraping.}

\item{torPort}{The port number for the local Tor SOCKS proxy (typically 9150).}

\item{wrong_ids_path}{Optional path to a CSV or Parquet file containing an
integer \code{WrongIDs} column listing IDs to skip.}

\item{timeout_sec}{Number of seconds before an individual HTTP request times
out. Defaults to 30.}

\item{delay_sec}{Seconds to pause between successive requests. Defaults to 1.}

\item{output_dir}{Directory where result files are written. Defaults to
\code{tempdir()}. Pass \code{NULL} to skip all file I/O and only return the data.}

\item{output_format}{File format for saving results: \code{"csv"} (default) or
\code{"parquet"}.}
}
\value{
A data frame containing scraped physician records, invisibly. When
\code{output_dir} is not \code{NULL}, result files are also written there and their
paths reported via \code{message()}.
}
\description{
Scrapes board-certification data for physicians within a specified ID range
from the ABOG API, routing all requests through a local Tor SOCKS proxy to
anonymise the source IP. IDs listed in \code{wrong_ids_path} are skipped.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Requires a running Tor proxy and network access.
scrape_physicians_data_with_tor(startID = 9045999, endID = 9046000, torPort = 9150)
\dontshow{\}) # examplesIf}
}
\seealso{
Other npi: 
\code{\link{tyler_get_clinician_data}()},
\code{\link{tyler_search_and_process_npi}()},
\code{\link{tyler_search_taxonomy}()}
}
\concept{npi}
