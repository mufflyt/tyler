% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irr_plot.R
\name{mysterycall_irr_plot}
\alias{mysterycall_irr_plot}
\title{IRR forest plot for a Poisson GLMER result}
\usage{
mysterycall_irr_plot(
  x,
  include_intercept = FALSE,
  reference_line = 1,
  point_size = 3,
  color_sig = "#C0392B",
  color_ns = "#2C3E50",
  x_label = "Incidence Rate Ratio (IRR)",
  title = NULL,
  x_log = FALSE
)
}
\arguments{
\item{x}{A \code{mysterycall_poisson_model} object from
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}, \strong{or} a data frame with columns \code{term},
\code{irr}, \code{ci_lower}, \code{ci_upper} (and optionally \code{p_value}).}

\item{include_intercept}{Logical. When \code{FALSE} (default) the \code{(Intercept)}
row is dropped before plotting.}

\item{reference_line}{Numeric. X-axis position for the null-effect vertical
line. Default \code{1} (IRR = 1).}

\item{point_size}{Numeric. Size of the IRR point. Default \code{3}.}

\item{color_sig}{Character. Colour for terms with \code{p_value < 0.05}. Default
\code{"#C0392B"} (red). Only applied when the \code{p_value} column is present.}

\item{color_ns}{Character. Colour for non-significant terms (or when
\code{p_value} is absent). Default \code{"#2C3E50"} (dark navy).}

\item{x_label}{Character. X-axis label. Default \code{"Incidence Rate Ratio (IRR)"}.}

\item{title}{Character. Plot title. \code{NULL} (default) produces no title.}

\item{x_log}{Logical. When \code{TRUE} the X axis is log-transformed so that
equal-ratio distances look equal. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object. Print it or save with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Produces a publication-ready forest plot showing each fixed-effect term as
an incidence rate ratio (point) with its Wald confidence interval (horizontal
bar). The intercept row is excluded by default. A vertical reference line
marks IRR = 1 (no effect). Requires only \code{ggplot2}, which is already in
the package Imports.
}
\examples{
if (requireNamespace("lme4", quietly = TRUE)) {
  set.seed(1978)
  df <- data.frame(
    wait_days = rpois(40, lambda = 18),
    insurance = rep(c("Medicaid", "BCBS"), each = 20),
    gender    = sample(c("Male", "Female"), 40, replace = TRUE),
    physician = rep(paste0("Dr_", 1:8), each = 5),
    stringsAsFactors = FALSE
  )
  result <- suppressWarnings(mysterycall_poisson_model(
    df, "wait_days", c("insurance", "gender"), "physician"
  ))
  mysterycall_irr_plot(result)
}
}
\seealso{
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}

Other outcomes: 
\code{\link{mysterycall_acceptance_rate}()},
\code{\link{mysterycall_marginal_effects}()},
\code{\link{mysterycall_model_metrics}()},
\code{\link{mysterycall_model_table}()},
\code{\link{mysterycall_plot_distribution}()},
\code{\link{mysterycall_plot_effect}()},
\code{\link{mysterycall_plot_emmeans_full}()},
\code{\link{mysterycall_plot_emmeans_interaction}()},
\code{\link{mysterycall_plot_inclexcl}()},
\code{\link{mysterycall_plot_residuals}()},
\code{\link{mysterycall_plot_sjplot_interaction}()},
\code{\link{mysterycall_plot_stacked_bar}()},
\code{\link{mysterycall_poisson_model}()},
\code{\link{mysterycall_save_plot}()},
\code{\link{mysterycall_screen_interactions}()},
\code{\link{mysterycall_select_best_model}()},
\code{\link{mysterycall_table1}()},
\code{\link{mysterycall_wait_time_summary}()}
}
\concept{outcomes}
