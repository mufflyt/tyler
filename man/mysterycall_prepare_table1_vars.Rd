% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_utils.R
\name{mysterycall_prepare_table1_vars}
\alias{mysterycall_prepare_table1_vars}
\title{Standardise demographic variables for Table 1}
\usage{
mysterycall_prepare_table1_vars(
  data,
  age_col = NULL,
  grad_year_col = NULL,
  gender_col = NULL,
  setting_col = NULL,
  region_col = NULL,
  ref_year = as.integer(format(Sys.Date(), "\%Y"))
)
}
\arguments{
\item{data}{A data frame.}

\item{age_col}{Optional character scalar naming an existing numeric age
column. When supplied, \code{age_category} is derived from it.}

\item{grad_year_col}{Optional character scalar naming a graduation-year
column. Used to impute age when \code{age_col} is \code{NULL}.}

\item{gender_col}{Optional character scalar naming a gender/sex column.
Values are recoded to \code{"Male"} (inputs \code{"m"} or \code{"male"}), \code{"Female"}
(inputs \code{"f"} or \code{"female"}), or \code{"Unknown"} for any other value
including \code{NA}. Matching is case-insensitive and whitespace-trimmed.
This is a \strong{binary classification}: non-binary or ambiguous values are
silently bucketed into \code{"Unknown"}.}

\item{setting_col}{Optional character scalar naming a practice-setting
column. Passed through as \code{setting_std}.}

\item{region_col}{Optional character scalar naming a region column. Passed
through as \code{region_std}.}

\item{ref_year}{Integer reference year for age imputation. Default: current
calendar year.}
}
\value{
\code{data} with zero or more additional standardised columns appended
(only columns whose source \verb{*_col} parameter was non-\code{NULL} are created):
\describe{
\item{\code{age_imputed}}{Numeric. Estimated age derived from \code{grad_year_col}
as \code{ref_year - grad_year}. Only added when \code{grad_year_col} is supplied.}
\item{\code{age_category}}{Character. Age group binned from \code{age_col} or
\code{age_imputed} (e.g., \code{"<35"}, \code{"35-44"}, \code{"45-54"}, \code{"55-64"},
\code{">=65"}). Only added when \code{age_col} or \code{grad_year_col} is supplied.}
\item{\code{gender_std}}{Character. Standardised gender: \code{"Male"}, \code{"Female"},
or \code{"Unknown"}. Only added when \code{gender_col} is supplied.}
\item{\code{setting_std}}{Character. Practice setting, copied from the source
column. Only added when \code{setting_col} is supplied.}
\item{\code{region_std}}{Character. Region, copied from the source column.
Only added when \code{region_col} is supplied.}
}
}
\description{
Convenience wrapper that applies age imputation, age categorisation, and
gender standardisation in one call.  All output columns are \strong{added} to
the input data frame - originals are never replaced or removed.  Arguments
set to \code{NULL} are silently skipped so you can standardise only the
variables your dataset contains.
}
\section{Gender standardisation}{

The \code{gender_std} column is produced by a binary lookup applied
case-insensitively after whitespace trimming:
\tabular{ll}{
\strong{Input value} \tab \strong{\code{gender_std}} \cr
\code{"male"}, \code{"m"}, \code{"Male"}, \code{"M"} \tab \code{"Male"} \cr
\code{"female"}, \code{"f"}, \code{"Female"}, \code{"F"} \tab \code{"Female"} \cr
\code{NA} \tab \code{"Unknown"} \cr
\code{""}, \code{"unknown"}, \code{"non-binary"}, any other string \tab \code{"Unknown"}
}
Non-binary values and any future Genderize.io API additions are all
mapped to \code{"Unknown"} silently. Before calling this function, inspect
\code{table(data[[gender_col]], useNA = "always")} to check for unexpected
values that will be bucketed into \code{"Unknown"}.
}

\examples{
df <- data.frame(
  grad_year = c(1990, 2000, 2010),
  gender    = c("Female", "M", "male")
)
mysterycall_prepare_table1_vars(df, grad_year_col = "grad_year",
                                 gender_col = "gender", ref_year = 2026L)
}
\seealso{
\code{\link[=mysterycall_genderize]{mysterycall_genderize()}} for the upstream API call that produces
the raw \code{gender} column; \code{\link[=mysterycall_preflight_check]{mysterycall_preflight_check()}} to validate
data quality before processing.

Other data management: 
\code{\link{mysterycall_check_duplicates}()},
\code{\link{mysterycall_check_generalist_presence}()},
\code{\link{mysterycall_extract_zip5}()},
\code{\link{mysterycall_luhn_check}()},
\code{\link{mysterycall_merge_with_prefix}()},
\code{\link{mysterycall_rename_columns}()},
\code{\link{mysterycall_stratified_sample}()}
}
\concept{data management}
