% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/green_journal.R
\name{save_green_journal_figure}
\alias{save_green_journal_figure}
\alias{save_publication_figure}
\title{Save a figure in Green Journal submission format}
\usage{
save_green_journal_figure(
  plot,
  path_stem,
  layout = c("double_column", "single_column"),
  height = NULL,
  plot_data = NULL,
  csv = TRUE
)

save_publication_figure(
  plot,
  path_stem,
  layout = c("double_column", "single_column"),
  height = NULL,
  plot_data = NULL,
  csv = TRUE
)
}
\arguments{
\item{plot}{A \code{ggplot} object.}

\item{path_stem}{Character. Path without extension
(e.g., \code{"figures/figure1"}). Extensions are added automatically.}

\item{layout}{Character. \code{"single_column"} (3.5 in) or
\code{"double_column"} (7.0 in). Controls width per Green Journal specs.}

\item{height}{Numeric. Height in inches. Default: \code{width * 0.7}, capped
at 9.0 in (Green Journal maximum).}

\item{plot_data}{Optional data frame for CSV export. \code{NULL} extracts
\code{plot$data}.}

\item{csv}{Logical. Export CSV (default \code{TRUE}).}
}
\value{
Invisible character vector of file paths written (PNG, PDF, and/or
CSV depending on arguments).
}
\description{
Exports a ggplot in all formats needed for journal submission:
\itemize{
\item \strong{TIFF} (300 DPI, LZW compression) -- primary submission format
\item \strong{PDF} (cairo, font-embedded) -- vector companion for review
\item \strong{PNG} (300 DPI) -- web / presentation companion
\item \strong{CSV} -- underlying data for peer-review transparency
}
}
\details{
Geometry columns (\code{sfc}) are automatically dropped from the CSV export.
Output directories are created recursively as needed.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point() + theme_green_journal()
save_green_journal_figure(p, "figures/fig1", layout = "double_column")
# Creates: figures/fig1.tiff  figures/fig1.pdf  figures/fig1.png
#          figures/fig1_data.csv
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=theme_green_journal]{theme_green_journal()}}, \code{\link[=theme_green_journal_map]{theme_green_journal_map()}} for themes
to apply before saving; \code{\link[=palette_green_journal]{palette_green_journal()}} for the colour palette.
}
\concept{green-journal-output}
