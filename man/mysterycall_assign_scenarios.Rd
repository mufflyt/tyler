% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_scenarios.R
\name{mysterycall_assign_scenarios}
\alias{mysterycall_assign_scenarios}
\title{Assign call scenarios to generalist providers}
\usage{
mysterycall_assign_scenarios(
  data,
  location_cols = c("city", "state_code"),
  specialty_col = "specialty_primary",
  generalist_level,
  scenario_map,
  id_col = NULL,
  scenario_col = "scenario"
)
}
\arguments{
\item{data}{A data frame containing provider records. One row = one provider.}

\item{location_cols}{Character vector of column names that define a
geographic group (default \code{c("city", "state_code")}). Rows are grouped by
all columns together.}

\item{specialty_col}{Character scalar. Column holding each provider's
specialty (default \code{"specialty_primary"}).}

\item{generalist_level}{Character scalar. The value in \code{specialty_col} that
identifies generalist providers who will receive scenario assignments.}

\item{scenario_map}{Named character vector mapping subspecialty values in
\code{specialty_col} to scenario labels. Example:
\code{c("Neurotology" = "Neurotology", "Pediatric Otolaryngology" = "Pediatric Otolaryngology")}.
Each name is a value that appears in \code{specialty_col}; each value is the
scenario label that will be assigned to generalists in cities where that
subspecialty is present.}

\item{id_col}{Optional character scalar. Column used to sort rows within each
location group before round-robin assignment. When \code{NULL} (default) the
existing row order is used.}

\item{scenario_col}{Character scalar. Name of the new scenario column to
create (default \code{"scenario"}).}
}
\value{
\code{data} with one additional character column named \code{scenario_col}.
}
\description{
In mystery caller studies, generalist providers are called using a vignette
that matches a subspecialty present in the same city/state. This function
groups providers by location, detects which subspecialty scenarios are
available locally, and assigns scenarios to generalists in round-robin order
so that calls are balanced across scenarios.
}
\details{
Non-generalist rows receive their own specialty as the scenario. Generalists
in locations that have no matching subspecialists receive \code{NA}.
}
\examples{
df <- data.frame(
  city          = c("Denver", "Denver", "Denver", "Denver", "Boulder", "Boulder"),
  state_code    = "CO",
  specialty_primary = c(
    "General Otolaryngology", "General Otolaryngology",
    "Neurotology", "Pediatric Otolaryngology",
    "General Otolaryngology", "Neurotology"
  ),
  id = 1:6,
  stringsAsFactors = FALSE
)
mysterycall_assign_scenarios(
  df,
  generalist_level = "General Otolaryngology",
  scenario_map     = c("Neurotology"               = "Neurotology",
                       "Pediatric Otolaryngology"  = "Pediatric Otolaryngology"),
  id_col           = "id"
)
}
\concept{study design}
