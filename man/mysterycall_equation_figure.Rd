% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_analysis.R
\name{mysterycall_equation_figure}
\alias{mysterycall_equation_figure}
\title{Poisson power curve: required sample size across a range of IRRs}
\usage{
mysterycall_equation_figure(
  lambda0 = 14,
  irr_seq = seq(1.1, 2, by = 0.05),
  alpha = 0.05,
  power = 0.8,
  both_arms = TRUE
)
}
\arguments{
\item{lambda0}{Numeric. Baseline expected count in the reference arm
(e.g. mean business days for the reference insurance). Default \code{14}.}

\item{irr_seq}{Numeric vector of IRR values to evaluate. Default
\code{seq(1.10, 2.0, by = 0.05)}.}

\item{alpha}{Numeric. Two-sided type I error rate. Default \code{0.05}.}

\item{power}{Numeric. Desired statistical power. Default \code{0.80}.}

\item{both_arms}{Logical. If \code{TRUE} (default), each arm receives \code{n}
patients; the total study size is \verb{2n}.}
}
\value{
A \code{ggplot} object. The x-axis is IRR, the y-axis is
required sample size per arm, and a vertical dashed line marks
IRR = 1 (no effect).
}
\description{
Calls \code{\link[=mysterycall_poisson_power]{mysterycall_poisson_power()}} across a range of incidence rate
ratios and returns a ggplot2 line chart showing how the required sample
size per arm changes with effect size. Useful as Figure 1 in a
mystery-caller manuscript.
}
\examples{
mysterycall_equation_figure(lambda0 = 14, irr_seq = seq(1.1, 1.8, 0.1))
}
\concept{sample size}
