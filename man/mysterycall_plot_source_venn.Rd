% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_source_venn.R
\name{mysterycall_plot_source_venn}
\alias{mysterycall_plot_source_venn}
\title{Three-circle Venn diagram for data-source overlap}
\usage{
mysterycall_plot_source_venn(
  data_or_path,
  A_col = "nppes",
  B_col = "pc",
  C_col = "dac",
  universe_col = "npis",
  source_short = c("NPPES", "PC", "DAC"),
  source_long = c("NPPES", "Physician Compare", "DAC"),
  fills = c("#2196F3", "#4CAF50", "#FF9800"),
  border_colors = c("#1565C0", "#2E7D32", "#E65100"),
  title = "Gender Data Availability Across Sources",
  subtitle = NULL,
  caption = NULL,
  output_path = NULL,
  width = 11,
  height = 9,
  dpi = 300L,
  bg = "white"
)
}
\arguments{
\item{data_or_path}{A named list whose elements are character or numeric
vectors of provider IDs, \strong{or} a file path to an \code{.rds} file containing
such a list.}

\item{A_col, B_col, C_col}{Character scalars naming the list elements that
correspond to circles A, B, and C respectively.
Defaults: \code{"nppes"}, \code{"pc"}, \code{"dac"}.}

\item{universe_col}{Character scalar naming the list element that contains
the complete universe of provider IDs.  When \code{NULL} the universe is taken
as \code{union(A, union(B, C))}.  Default \code{"npis"}.}

\item{source_short}{Character vector of length 3.  Short labels used inside
the intersection region annotations (e.g. \code{"NPPES + PC"}).}

\item{source_long}{Character vector of length 3.  Full labels used in the
bold header annotations above/below each circle.}

\item{fills}{Character vector of length 3.  Fill colours for circles A, B,
C (hex or R colour names).}

\item{border_colors}{Character vector of length 3.  Border and text colours
matching each circle.}

\item{title, subtitle, caption}{Character scalars for plot text.  When
\code{subtitle} or \code{caption} is \code{NULL} a sensible default is auto-generated
from the counts.}

\item{output_path}{Optional file path.  When supplied the plot is saved with
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.  The file format is inferred from the extension
(\code{.png}, \code{.pdf}, \code{.svg}, etc.).}

\item{width, height}{Numeric.  Plot dimensions in inches passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.  Defaults \code{11} × \code{9}.}

\item{dpi}{Integer.  Resolution for raster output.  Default \code{300}.}

\item{bg}{Character scalar.  Background colour passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.  Default \code{"white"}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object (invisibly when \code{output_path} is
supplied).
}
\description{
Visualises how three data sources (A, B, C) cover a universe of providers,
labelling each region with its count and the centre with the three-way
intersection count and percentage.  The result is a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}
object, optionally written to disk.
}
\section{Data format}{

The input list must have at minimum three named elements whose values are
vectors of provider identifiers (typically NPI strings).  Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{d <- list(
  npis  = c("A","B","C","D","E"),   # universe
  nppes = c("A","B","C"),
  pc    = c("B","C","D"),
  dac   = c("C","D","E")
)
mysterycall_plot_source_venn(d)
}\if{html}{\out{</div>}}
}

\examples{
\dontshow{if (interactive() && requireNamespace("ggforce", quietly = TRUE)) withAutoprint(\{ # examplesIf}
d <- list(
  npis  = as.character(1:100),
  nppes = as.character(1:70),
  pc    = as.character(30:90),
  dac   = as.character(50:100)
)
p <- mysterycall_plot_source_venn(d, title = "Demo Venn Diagram")
print(p)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[ggforce:geom_circle]{ggforce::geom_circle()}}
}
\concept{plotting}
