% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_phase_2_results.R
\name{mysterycall_clean_phase2}
\alias{mysterycall_clean_phase2}
\title{Clean and process Phase 2 data}
\usage{
mysterycall_clean_phase2(
  data_or_path,
  required_strings,
  standard_names,
  output_directory = NULL,
  output_format = c("csv", "parquet")
)
}
\arguments{
\item{data_or_path}{Path to the data file or a data frame.}

\item{required_strings}{Vector of substrings for which to search in column names.}

\item{standard_names}{Character vector of replacement names, the same length
as \code{required_strings}, applied to matched columns in order.}

\item{output_directory}{Directory where the cleaned dataset should be written.
Defaults to a session-specific folder inside \code{\link[=tempdir]{tempdir()}} when not provided.}

\item{output_format}{File format to use when persisting the cleaned dataset.
Supported values are "csv" (default) and "parquet".}
}
\value{
A data frame with processed data.
}
\description{
This function reads data from a file or data frame, cleans column names, and applies renaming based on specified criteria to facilitate data analysis. The function logs each step of the process, including data loading, column cleaning, and renaming for transparency.
}
\section{Column name transformation}{

All input column names are converted to \strong{lowercase snake_case} by
\code{\link[janitor:clean_names]{janitor::clean_names()}} \strong{unconditionally} before \code{required_strings}
pattern matching. Transformation cannot be disabled. Common examples:
\tabular{ll}{
\strong{Input column name} \tab \strong{After \code{clean_names()}} \cr
\code{"Physician Information"} \tab \code{"physician_information"} \cr
\code{"NPI Number"} \tab \code{"npi_number"} \cr
\code{"Q1-2023 Revenue"} \tab \code{"q1_2023_revenue"} \cr
\code{"able_to_contact_office"} \tab \code{"able_to_contact_office"} (unchanged) \cr
\code{"ContactOfficeYN"} \tab \code{"contact_office_yn"}
}
To preview the exact names your data will have, run
\code{names(janitor::clean_names(your_data))} before calling this function,
then write \code{required_strings} to match those transformed names.
}

\section{Output file timestamps}{

Output filenames include a timestamp from \code{\link[=Sys.time]{Sys.time()}}, which uses the
\strong{local system timezone} (not UTC). Filenames produced on systems in
different timezones will reflect different local times for the same
wall-clock moment. To force UTC filenames, wrap the call:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{withr::with_timezone("UTC", \{
  mysterycall_clean_phase2(data, required_strings, standard_names)
\})
}\if{html}{\out{</div>}}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Assuming an input path to a CSV file
input_path <- "path_to_your_data.csv"
required_strings <- c("physician_information", "able_to_contact_office")
standard_names <- c("physician_info", "contact_office")
cleaned_data <- mysterycall_clean_phase2(input_path, required_strings, standard_names)

# Directly using a data frame
df <- data.frame(
  doc_info = 1:5,
  contact_data = 6:10
)
required_strings <- c("doc_info", "contact_data")
standard_names <- c("doctor_info", "patient_contact_info")
cleaned_df <- mysterycall_clean_phase2(df, required_strings, standard_names)
print(cleaned_df)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}} for Phase 1 data cleaning;
\code{\link[=mysterycall_run_workflow]{mysterycall_run_workflow()}} which orchestrates both phases;
\code{\link[=mysterycall_rename_columns]{mysterycall_rename_columns()}} for the underlying column-renaming logic.

Other workflow: 
\code{\link{mysterycall_call_productivity}()},
\code{\link{mysterycall_clean_phase1}()},
\code{\link{mysterycall_print_dashboard}()},
\code{\link{mysterycall_run_workflow}()},
\code{\link{mysterycall_run_workflow_logged}()},
\code{\link{mysterycall_split_and_save}()},
\code{\link{mysterycall_verify_artifact}()}
}
\concept{workflow}
