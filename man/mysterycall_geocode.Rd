% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode.R
\name{mysterycall_geocode}
\alias{mysterycall_geocode}
\title{Geocode unique addresses from a file}
\usage{
mysterycall_geocode(
  file_path,
  google_maps_api_key,
  output_file_path = NULL,
  failed_output_path = NULL,
  notify = TRUE,
  quiet = getOption("mysterycall.quiet", FALSE),
  tracker = NULL,
  tracker_step = "Geocoding"
)
}
\arguments{
\item{file_path}{Path to a CSV, RDS or XLSX file containing an \code{address}
column.}

\item{google_maps_api_key}{A valid Google Maps API key.}

\item{output_file_path}{Optional path to save the geocoded dataset as CSV.}

\item{failed_output_path}{Optional path that captures rows that failed to
geocode after all retries. When supplied, a timestamped backup is created
before overwriting existing results.}

\item{notify}{Logical. If \code{TRUE}, play a notification sound when geocoding
finishes (requires the optional \code{beepr} package). Defaults to \code{TRUE}.}

\item{quiet}{Logical flag controlling log verbosity. Defaults to the package
quiet-mode option.}

\item{tracker}{Optional progress tracker created with \code{\link[=mysterycall_progress_tracker]{mysterycall_progress_tracker()}}.
When supplied, the step named by \code{tracker_step} is automatically started and
marked as complete or failed with an appropriate quality tier.}

\item{tracker_step}{Character string describing the step name used when
updating \code{tracker}.}
}
\value{
A data frame with latitude and longitude columns added.
}
\description{
Reads an input dataset containing an \code{address} column, geocodes the unique
addresses using the Google Maps API via \code{ggmap::geocode}, and returns the
data with additional \code{latitude} and \code{longitude} columns. Optionally writes the
result to \code{output_file_path}.
}
\section{Requirements}{

Geocoding requires the \code{ggmap} package (listed in \code{Suggests}, not
\code{Imports}) and a Google Maps Platform API key with the \strong{Geocoding API}
enabled. Steps to obtain a key:
\enumerate{
\item Create a project at \url{https://console.cloud.google.com/}.
\item Enable the \strong{Geocoding API} under APIs & Services → Library.
\item Create a key under APIs & Services → Credentials.
\item Enable \strong{billing} on the project (required even for free-tier usage;
Google provides a $200/month credit that covers ~40,000 geocodes).
}

Common errors and remedies:
\tabular{ll}{
\strong{Error message} \tab \strong{Cause / fix} \cr
\code{"ggmap is required"} \tab Run \code{install.packages("ggmap")}. \cr
\code{"REQUEST_DENIED"} \tab Geocoding API not enabled for the key. \cr
\code{"OVER_QUERY_LIMIT"} \tab Daily free quota (40,000 calls) exceeded. \cr
\code{"INVALID_REQUEST"} \tab Malformed address string in the data.
}

An invalid or expired key is only detected at the first geocoding
request, not at function entry. Run \code{\link[=mysterycall_preflight_check]{mysterycall_preflight_check()}} with
\code{check_apis = TRUE} before a long workflow to catch key problems early.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
result <- mysterycall_geocode("addresses.csv", "my_api_key")
\dontshow{\}) # examplesIf}
}
\seealso{
Other geospatial helpers: 
\code{\link{mysterycall_calculate_overlap}()},
\code{\link{mysterycall_hrr}()}
}
\concept{geospatial helpers}
