% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_by_taxonomy.R
\name{mysterycall_search_taxonomy}
\alias{mysterycall_search_taxonomy}
\title{Search NPI Database by Taxonomy}
\usage{
mysterycall_search_taxonomy(
  taxonomy_to_search,
  states = NULL,
  city = NULL,
  limit = 1200L,
  write_snapshot = TRUE,
  snapshot_dir = NULL,
  notify = TRUE
)
}
\arguments{
\item{taxonomy_to_search}{A character vector containing the taxonomy
description(s) to search for.}

\item{states}{Optional character vector of two-letter US state abbreviations
(e.g. \code{c("CO", "CA", "NY")}). When supplied the search is repeated for
each state so that all providers are captured even for large specialties
that exceed the 1\,200-record per-query limit. Pass all 50 state
abbreviations to perform a complete national search. Defaults to \code{NULL}
(national search, capped at \code{limit} records).}

\item{city}{Optional city name passed to \code{\link[npi:npi_search]{npi::npi_search()}}.}

\item{limit}{Maximum records to request per API call. Capped at 1\,200 by
the NPI API. Defaults to \code{1200L}.}

\item{write_snapshot}{Logical. If \code{TRUE}, the retrieved data is saved as an
\code{.rds} file for later reference. Defaults to \code{TRUE}.}

\item{snapshot_dir}{Directory where snapshot files should be written when
\code{write_snapshot} is \code{TRUE}. Defaults to a session-specific folder inside
\code{\link[=tempdir]{tempdir()}} when not supplied.}

\item{notify}{Logical. If \code{TRUE}, play a notification sound when processing
completes (requires the optional \code{beepr} package). Defaults to \code{TRUE}.}
}
\value{
A data frame with filtered NPI data based on the specified taxonomy
description.
}
\description{
This function searches the NPI Database for healthcare providers based on a
taxonomy description. The NPI API returns at most 1\,200 records per query.
To retrieve all providers for a large specialty, supply a character vector
of state abbreviations via the \code{states} argument; the function will loop over
each state and combine the results, effectively bypassing the 1\,200-record
cap.
}
\section{Output columns}{

The function returns a curated subset of \code{npi::npi_flatten()} output.

\strong{Retained} (after renaming):
\code{npi}, \code{first_name}, \code{last_name}, \code{middle_name}, \code{credential},
\code{addresses_city}, \code{addresses_state}, \code{addresses_postal_code},
\code{addresses_telephone_number}, \code{addresses_country_name},
\code{taxonomies_desc}, \code{taxonomies_primary}, \code{search_term}.
Any columns returned by \code{npi_flatten()} that are \emph{not} on the explicit
drop list are also retained.

\strong{Always dropped} regardless of what the NPI API returns:
\itemize{
\item All \verb{endpoints_*} fields (electronic health endpoint metadata)
\item All \verb{other_names_*} fields (former or alternate name records)
\item All \verb{identifiers_*} fields (legacy identifier records)
\item Supplementary address fields: \code{addresses_address_2},
\code{addresses_fax_number}, \code{addresses_address_type},
\code{addresses_country_code}
\item Basic metadata: \code{basic_last_updated}, \code{basic_status},
\code{basic_name_prefix}, \code{basic_name_suffix},
\code{basic_certification_date}
\item Secondary taxonomy fields: \code{taxonomies_code},
\code{taxonomies_taxonomy_group}, \code{taxonomies_state},
\code{taxonomies_license}
\item Internal working column: \code{credential_lower}
}

To access dropped columns, call \code{npi::npi_search()} and
\code{npi::npi_flatten()} directly before any filtering.
}

\section{Contract}{

\strong{Inputs:}
\itemize{
\item \code{taxonomy_to_search} must be an exact NUCC taxonomy description string
(see \code{mysterycall::taxonomy} dataset for valid values).
\item NPI registry API must be reachable; the function retries up to 3 times with
exponential back-off before returning an empty data frame.
}

\strong{Guarantees:}
\itemize{
\item Returns a zero-row data frame (never \code{NULL}) when no records are found.
\item Output rows are deduplicated on NPI.
\item When \code{write_snapshot = TRUE}, an \code{.rds} file is written to \code{output_dir}
for reproducible re-runs without re-querying the live API.
}

\strong{Fails if:}
\itemize{
\item Network is unavailable after all retries (returns empty data frame, does
not error).
\item \code{states} contains an invalid two-letter abbreviation (silently skipped).
}
}

\section{Performance}{

O(t * p / 200) HTTP requests where \code{t} = number of state batches and
\code{p} = max_records (default 1,200). Each page request targets < 200 rows.
Full national search across all 50 states for one taxonomy takes ~2–5 min
depending on registry load. Results are cached in-memory per session when
\code{use_cache = TRUE}.
}

\section{Called By}{

\itemize{
\item \code{\link[=mysterycall_run_workflow]{mysterycall_run_workflow()}}
\item \code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}} (indirectly via taxonomy lookup)
}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# National search (limited to 1200 records per taxonomy):
go_data <- mysterycall_search_taxonomy("Gynecologic Oncology")

# Full national search by looping over every state:
all_states <- c(
  "AL","AK","AZ","AR","CA","CO","CT","DE","FL","GA",
  "HI","ID","IL","IN","IA","KS","KY","LA","ME","MD",
  "MA","MI","MN","MS","MO","MT","NE","NV","NH","NJ",
  "NM","NY","NC","ND","OH","OK","OR","PA","RI","SC",
  "SD","TN","TX","UT","VT","VA","WA","WV","WI","WY"
)
fpmrs_data <- mysterycall_search_taxonomy(
  "Female Pelvic Medicine and Reconstructive Surgery",
  states = all_states
)
\dontshow{\}) # examplesIf}
}
\seealso{
Other npi: 
\code{\link{mysterycall_get_clinician_data}()},
\code{\link{mysterycall_search_and_process_npi}()}
}
\concept{npi}
