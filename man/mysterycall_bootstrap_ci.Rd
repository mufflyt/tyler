% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_ci.R
\name{mysterycall_bootstrap_ci}
\alias{mysterycall_bootstrap_ci}
\title{Bootstrap Confidence Intervals for a Summary Statistic}
\usage{
mysterycall_bootstrap_ci(
  data,
  outcome_col,
  group_col = NULL,
  n_boot = 2000L,
  seed = NULL,
  alpha = 0.05,
  stat = c("proportion", "mean", "median")
)
}
\arguments{
\item{data}{A data.frame.}

\item{outcome_col}{Character scalar. Name of the outcome column.
When \code{stat = "proportion"}, values must be 0/1.}

\item{group_col}{Character scalar or NULL. Name of a grouping column.
If NULL, a single "Overall" row is returned.}

\item{n_boot}{Positive integer (>= 100). Number of bootstrap replicates.
Default 2000L.}

\item{seed}{Integer or NULL. If not NULL, passed to \code{set.seed()}
before bootstrapping to make results reproducible.}

\item{alpha}{Numeric in (0, 1). CI uses the \code{alpha/2} and
\code{1 - alpha/2} percentiles. Default 0.05.}

\item{stat}{Character. Statistic to bootstrap. One of
\code{"proportion"} (mean of a 0/1 variable), \code{"mean"}, or \code{"median"}.}
}
\value{
A data frame with one row per group (or \code{"Overall"} when
\code{group_col} is \code{NULL}) and columns:
\describe{
\item{\code{group}}{Character. Group label, or \code{"Overall"} when
\code{group_col} is \code{NULL}.}
\item{\code{n}}{Integer. Number of non-NA observations used.}
\item{\code{estimate}}{Numeric. Observed statistic value (proportion,
mean, or median depending on \code{stat}).}
\item{\code{lower_ci}}{Numeric. Lower percentile CI bound at \code{alpha/2}.}
\item{\code{upper_ci}}{Numeric. Upper percentile CI bound at
\code{1 - alpha/2}.}
\item{\code{n_boot}}{Integer. Number of bootstrap replicates used.}
}
}
\description{
For each level of an optional grouping column (or for the whole dataset),
draws \code{n_boot} bootstrap samples with replacement, computes a
summary statistic, and returns percentile confidence intervals.
}
\examples{
set.seed(42)
df <- data.frame(
  insurance = sample(c("Medicaid", "Private"), 200, replace = TRUE),
  accepted  = rbinom(200, 1, 0.6)
)
mysterycall_bootstrap_ci(df, "accepted", group_col = "insurance", seed = 1)

}
\seealso{
\code{\link[=mysterycall_acceptance_rate]{mysterycall_acceptance_rate()}}, \code{\link[=mysterycall_wait_time_summary]{mysterycall_wait_time_summary()}}
for model-based outcome summaries; \code{\link[=mysterycall_compare_waves]{mysterycall_compare_waves()}} for
cross-wave significance testing.

Other inference: 
\code{\link{mysterycall_compare_waves}()},
\code{\link{mysterycall_multiple_comparison_adjust}()}
}
\concept{inference}
