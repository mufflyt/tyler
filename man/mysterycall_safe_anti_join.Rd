% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_safety.R
\name{mysterycall_safe_anti_join}
\alias{mysterycall_safe_anti_join}
\title{Safe anti join with over-exclusion guard}
\usage{
mysterycall_safe_anti_join(
  left,
  right,
  by,
  label_left = "left",
  label_right = "right",
  max_matched = NULL,
  write_report = FALSE,
  report_prefix = "join_anti"
)
}
\arguments{
\item{left}{A data frame. All rows are preserved.}

\item{right}{A data frame. The lookup / enrichment table.}

\item{by}{Character vector or \code{\link[dplyr:join_by]{dplyr::join_by()}} specifying join keys.
Required; use named vector for cross-column mapping
(\code{c("npi" = "provider_npi")}).}

\item{label_left, label_right}{Character scalars used in messages and
the audit report.}

\item{max_matched}{Numeric in \verb{[0, 1]}. Maximum fraction of left rows that
may be matched (and thus excluded). Default \code{NULL} -> reads
\code{JOIN_MAX_MATCHED} env var or \code{1.0}.}

\item{write_report}{Logical. Write a one-row CSV audit record. Default
\code{FALSE}.}

\item{report_prefix}{Character scalar. Filename prefix for the audit CSV.}
}
\value{
A data frame: the unmatched subset of \code{left}.
}
\description{
Wraps \code{\link[dplyr:filter-joins]{dplyr::anti_join()}} (return left rows with \strong{no} match in right)
with key validation, type harmonisation, and an optional cap on how many
rows may be matched away.
}
\details{
\code{max_matched} is the maximum fraction of left rows that may be excluded.
Default \code{1.0} (no limit); reads \code{JOIN_MAX_MATCHED} env var.
}
\examples{
all_physicians <- data.frame(npi = c("1","2","3","4"), stringsAsFactors = FALSE)
retired        <- data.frame(npi = c("2","4"),         stringsAsFactors = FALSE)

active <- mysterycall_safe_anti_join(
  left         = all_physicians,
  right        = retired,
  by           = "npi",
  label_left   = "all_physicians",
  label_right  = "retired_list",
  max_matched  = 0.60,
  write_report = FALSE
)

}
\seealso{
\code{\link[=mysterycall_safe_left_join]{mysterycall_safe_left_join()}}, \code{\link[=mysterycall_safe_inner_join]{mysterycall_safe_inner_join()}},
\code{\link[=mysterycall_safe_semi_join]{mysterycall_safe_semi_join()}}, \code{\link[=mysterycall_assert_unique_keys]{mysterycall_assert_unique_keys()}}

Other safe-joins: 
\code{\link{mysterycall_assert_unique_keys}()},
\code{\link{mysterycall_safe_inner_join}()},
\code{\link{mysterycall_safe_left_join}()},
\code{\link{mysterycall_safe_semi_join}()}
}
\concept{safe-joins}
