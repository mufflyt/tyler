% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_and_remove_invalid_npi.R
\name{mysterycall_validate_npi}
\alias{mysterycall_validate_npi}
\title{Validate and Remove Invalid NPI Numbers}
\usage{
mysterycall_validate_npi(input_data)
}
\arguments{
\item{input_data}{Either a data frame containing NPI numbers or a path to a CSV file.}
}
\value{
A data frame containing valid NPI numbers, with invalid or missing NPI rows removed.
}
\description{
This function reads a CSV file containing NPI numbers, validates their
format using the npi package, and removes rows with missing or invalid NPIs.
}
\section{Contract}{

\strong{Inputs:}
\itemize{
\item \code{input_data} must contain an \code{npi} column (character or numeric).
\item NPIs are validated via the Luhn checksum algorithm (NPI standard).
}

\strong{Guarantees:}
\itemize{
\item Output rows \eqn{\subseteq} input rows — rows are only removed, never added.
\item Every removed row had either \code{NA}, a non-10-digit string, or a failed Luhn
checksum.
\item Output NPI column is always character type.
}

\strong{Disaster Prevention:}
Prevents silent retention of placeholder NPIs (e.g., all-zeros or
test values such as "1234567890") that would match real providers in
downstream joins and inflate match rates.
}

\section{Performance}{

O(n) with a small constant; Luhn validation is a pure arithmetic check.
Expect < 0.1 s for 10,000 rows.
}

\section{Called By}{

\itemize{
\item \code{\link[=mysterycall_run_workflow]{mysterycall_run_workflow()}}
\item \code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}}
}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
df <- data.frame(npi = c("1234567893", "0000000000", NA_character_))
mysterycall_validate_npi(df)
\dontshow{\}) # examplesIf}
}
