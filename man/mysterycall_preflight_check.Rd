% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preflight_checks.R
\name{mysterycall_preflight_check}
\alias{mysterycall_preflight_check}
\title{Run comprehensive preflight checks before workflow}
\usage{
mysterycall_preflight_check(
  input_data,
  output_dir,
  google_maps_api_key = NULL,
  here_api_key = NULL,
  check_apis = TRUE,
  estimate_resources = TRUE,
  prompt_user = interactive(),
  interactive = NULL,
  required_columns = c("first", "last")
)
}
\arguments{
\item{input_data}{Either a file path (character scalar) to a CSV/Parquet
roster, or an already-loaded data frame. If a path, the file must exist and
be readable; if a data frame it is validated in memory.}

\item{output_dir}{Character scalar. Destination directory for workflow
outputs. Created if it does not exist.}

\item{google_maps_api_key}{Character scalar. Google Maps Platform API key.
Required only when geocoding is part of the workflow; leave \code{NULL}
to skip geocoding validation.}

\item{here_api_key}{Character scalar. HERE Routing API key. Required only
when creating drive-time isochrones; leave \code{NULL} to skip.}

\item{check_apis}{Logical. If \code{TRUE} (default), validates each non-\code{NULL}
API key by making a minimal test request. Set to \code{FALSE} to skip
live validation (e.g., in tests or when offline).}

\item{estimate_resources}{Logical. If \code{TRUE} (default), estimates wall-clock
time and peak memory consumption before the run starts.}

\item{prompt_user}{Logical. If \code{TRUE} (default in interactive sessions),
prints a go/no-go summary and waits for user confirmation. Set to
\code{FALSE} in non-interactive scripts.}

\item{interactive}{Alias for \code{prompt_user}; if non-\code{NULL} it takes
precedence over \code{prompt_user}. Provided for back-compatibility with
scripts that pass \code{interactive = FALSE}.}

\item{required_columns}{Character vector of column names that must be present
in \code{input_data}. Defaults to \code{c("first", "last")}.}
}
\value{
\code{invisible()} list with elements: \code{passed} (logical), \code{checks}
(list of individual check results), \code{errors} (character vector of
blocking issues), \code{warnings} (character vector of non-blocking issues),
\code{data} (the validated input data frame), \code{n_rows} (integer), and
\code{estimates} (list from \code{\link[=mysterycall_estimate_resources]{mysterycall_estimate_resources()}}, or \code{NULL}
when \code{estimate_resources = FALSE}). Stops with an informative error if
any required check fails.
}
\description{
Validates all requirements for a successful workflow run including API keys,
data quality, output directories, estimated resources, and dependencies.
Provides a go/no-go decision before starting long-running operations.
}
\section{Data quality thresholds}{

Input data is scored from 0-1 by \code{\link[=mysterycall_assess_data_quality]{mysterycall_assess_data_quality()}}.
Scores below \strong{0.70} cause this function to stop with an error; scores
between 0.70 and 0.80 emit a warning. A score below 0.70 typically means
a required name column has more than 50\\% missing values. See
\code{\link[=mysterycall_assess_data_quality]{mysterycall_assess_data_quality()}} for the full penalty schedule and
worked examples.

The thresholds are \strong{not configurable} via parameters. To investigate
quality issues without stopping, call \code{\link[=mysterycall_assess_data_quality]{mysterycall_assess_data_quality()}}
directly and inspect the returned \verb{$issues} list:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{report <- mysterycall_assess_data_quality(my_data)
report$score          # numeric 0-1
report$issues         # list of issue records with $severity and $message
}\if{html}{\out{</div>}}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Basic preflight check
mysterycall_preflight_check(
  input_data = "physicians.csv",
  output_dir = "output/",
  google_maps_api_key = Sys.getenv("GOOGLE_API_KEY"),
  here_api_key = Sys.getenv("HERE_API_KEY")
)

# Non-interactive (for scripts)
mysterycall_preflight_check(
  input_data = data,
  output_dir = "output/",
  interactive = FALSE
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_assess_data_quality]{mysterycall_assess_data_quality()}} for the scoring formula and
per-issue details; \code{\link[=mysterycall_estimate_resources]{mysterycall_estimate_resources()}} for runtime and
memory projections.

Other utilities: 
\code{\link{\%>\%}()},
\code{\link{mysterycall_assess_data_quality}()},
\code{\link{mysterycall_check_api_response}()},
\code{\link{mysterycall_check_data_completeness}()},
\code{\link{mysterycall_check_dependencies}()},
\code{\link{mysterycall_check_no_data_loss}()},
\code{\link{mysterycall_check_no_limits}()},
\code{\link{mysterycall_download_file}()},
\code{\link{mysterycall_estimate_resources}()},
\code{\link{mysterycall_export_with_backup}()},
\code{\link{mysterycall_quality_tier}()},
\code{\link{mysterycall_resolve_path}()},
\code{\link{mysterycall_save_quality_table}()},
\code{\link{mysterycall_scan_for_limits}()},
\code{\link{mysterycall_standard_labels}()},
\code{\link{mysterycall_standard_palette}()}
}
\concept{utilities}
