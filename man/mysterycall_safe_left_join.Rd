% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_safety.R
\name{mysterycall_safe_left_join}
\alias{mysterycall_safe_left_join}
\title{Safe left join with coverage validation}
\usage{
mysterycall_safe_left_join(
  left,
  right,
  by,
  expect_unique_right = TRUE,
  label_left = "left",
  label_right = "right",
  min_coverage = NULL,
  max_duplication = NULL,
  suffix = c(".x", ".y"),
  write_report = FALSE,
  report_prefix = "join_left"
)
}
\arguments{
\item{left}{A data frame. All rows are preserved.}

\item{right}{A data frame. The lookup / enrichment table.}

\item{by}{Character vector or \code{\link[dplyr:join_by]{dplyr::join_by()}} specifying join keys.
Required; use named vector for cross-column mapping
(\code{c("npi" = "provider_npi")}).}

\item{expect_unique_right}{Logical. When \code{TRUE} (default) right-side keys
must be unique; stops if duplicates are detected.}

\item{label_left, label_right}{Character scalars used in messages and
the audit report.}

\item{min_coverage}{Numeric in \verb{[0, 1]}. Minimum fraction of left rows that
must find a match. Default \code{0.98} (reads \code{JOIN_MIN_COVERAGE} env var).}

\item{max_duplication}{Numeric >= 0. Maximum allowed output/input row ratio.
Default \code{1.02} (reads \code{JOIN_MAX_DUPLICATION} env var).}

\item{suffix}{Character vector of length 2. Suffixes for conflicting column
names. Default \code{c(".x", ".y")}.}

\item{write_report}{Logical. Write a one-row CSV audit record. Default
\code{FALSE}.}

\item{report_prefix}{Character scalar. Filename prefix for the audit CSV.}
}
\value{
A data frame: the left-join result.
}
\description{
Wraps \code{\link[dplyr:mutate-joins]{dplyr::left_join()}} with:
\itemize{
\item Key-type harmonisation (prevents silent 0-match joins after type
coercion from RDS or Parquet round-trips).
\item Right-side uniqueness assertion (blocks accidental many-to-many
fan-outs).
\item Coverage threshold enforcement -- stops if fewer than
\code{min_coverage} of left rows find a match.
\item Optional CSV audit report written to \code{JOIN_REPORT_DIR} env var or
\code{getOption("mysterycall.join_report_dir", tempdir())}.
}
}
\details{
Coverage defaults and env-var overrides:
\itemize{
\item \code{min_coverage} default \code{0.98}; override with \code{JOIN_MIN_COVERAGE}.
\item \code{max_duplication} default \code{1.02}; override with \code{JOIN_MAX_DUPLICATION}.
}
}
\examples{
physicians <- data.frame(
  npi       = c("1", "2", "3"),
  specialty = c("OB", "GYN", "RE"),
  stringsAsFactors = FALSE
)
demographics <- data.frame(
  npi  = c("1", "2"),
  state = c("CO", "TX"),
  stringsAsFactors = FALSE
)

result <- mysterycall_safe_left_join(
  left         = physicians,
  right        = demographics,
  by           = "npi",
  label_left   = "physicians",
  label_right  = "demographics",
  min_coverage = 0.50,
  write_report = FALSE
)

}
\seealso{
\code{\link[=mysterycall_safe_inner_join]{mysterycall_safe_inner_join()}}, \code{\link[=mysterycall_safe_semi_join]{mysterycall_safe_semi_join()}},
\code{\link[=mysterycall_safe_anti_join]{mysterycall_safe_anti_join()}}, \code{\link[=mysterycall_assert_unique_keys]{mysterycall_assert_unique_keys()}}

Other safe-joins: 
\code{\link{mysterycall_assert_unique_keys}()},
\code{\link{mysterycall_safe_anti_join}()},
\code{\link{mysterycall_safe_inner_join}()},
\code{\link{mysterycall_safe_semi_join}()}
}
\concept{safe-joins}
