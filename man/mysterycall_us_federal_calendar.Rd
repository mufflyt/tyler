% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/business_days.R
\name{mysterycall_us_federal_calendar}
\alias{mysterycall_us_federal_calendar}
\title{Build a US federal holiday calendar}
\usage{
mysterycall_us_federal_calendar(
  start_year = as.integer(format(Sys.Date(), "\%Y")) - 5L,
  end_year = as.integer(format(Sys.Date(), "\%Y")) + 10L
)
}
\arguments{
\item{start_year}{First year to include. Defaults to five years before the
current year, which covers retrospective data collection.}

\item{end_year}{Last year to include. Defaults to ten years after the
current year.}
}
\value{
An S3 object of class \code{"calendar"} (from \code{bizdays}) named
\code{"USFederal"}. Pass it to \code{\link[bizdays:bizdays]{bizdays::bizdays()}} or \code{\link[bizdays:is.bizday]{bizdays::is.bizday()}}
for date arithmetic. Contains one holiday entry per year from
\code{start_year} to \code{end_year}, with fixed holidays observing the
Saturday-to-Friday / Sunday-to-Monday shift rule.
}
\description{
Creates a \code{\link[bizdays:create.calendar]{bizdays::create.calendar()}} object covering all 11 US federal
holidays for the requested year range. Fixed holidays are shifted to their
observed weekday (Friday if they fall on Saturday, Monday if Sunday).
Floating holidays (MLK Day, Presidents Day, Memorial Day, Labor Day,
Columbus Day, Thanksgiving) are computed exactly.
}
\details{
The calendar is used internally by \code{\link[=mysterycall_business_days]{mysterycall_business_days()}} and
\code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}}. You only need to call this directly
if you want to reuse the same calendar object across many calls for
performance, or if you need to inspect the holiday list.
}
\examples{
\dontshow{if (requireNamespace("bizdays", quietly = TRUE)) withAutoprint(\{ # examplesIf}
cal <- mysterycall_us_federal_calendar(2024, 2028)
bizdays::is.bizday(as.Date("2026-01-19"), cal)  # MLK Day -> FALSE
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_business_days]{mysterycall_business_days()}}, \code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}}

Other business days: 
\code{\link{mysterycall_business_days}()},
\code{\link{mysterycall_count_business_days}()}
}
\concept{business days}
