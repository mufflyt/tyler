% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_scatter_plot.R
\name{create_scatter_plot}
\alias{create_scatter_plot}
\title{Create a Scatter Plot for Mystery Caller Studies with Optional Transformations, Jitter, and Custom Labels}
\usage{
create_scatter_plot(
  plot_data,
  x_var,
  y_var,
  y_transform = "none",
  dpi = 600,
  output_dir = NULL,
  file_prefix = "scatter_plot",
  jitter_width = 0.2,
  jitter_height = 0,
  point_alpha = 0.6,
  x_label = NULL,
  y_label = NULL,
  plot_title = NULL,
  verbose = TRUE
)
}
\arguments{
\item{plot_data}{A dataframe containing the data to be plotted. Must contain the variables specified in \code{x_var} and \code{y_var}.}

\item{x_var}{A string representing the column name for the x-axis variable. This should be a categorical or factor variable (e.g., insurance type).}

\item{y_var}{A string representing the column name for the y-axis variable. This should be a numeric variable (e.g., waiting time in days).}

\item{y_transform}{A string specifying the transformation for the y-axis: "log" for log transformation (log1p), "sqrt" for square root transformation, or "none" for no transformation. Default is "none".}

\item{dpi}{An integer specifying the resolution of the saved plot in dots per inch (DPI). Default is 600 for print-ready outputs.}

\item{output_dir}{A string representing the directory where the plot files will be
saved. Defaults to a session-specific folder inside \code{\link[=tempdir]{tempdir()}}.}

\item{file_prefix}{A string used as the prefix for the generated plot filenames. The filenames will have a timestamp appended to ensure uniqueness. Default is "scatter_plot".}

\item{jitter_width}{A numeric value specifying the width of the jitter along the x-axis. Default is 0.2.}

\item{jitter_height}{A numeric value specifying the height of the jitter along the y-axis. Default is 0.}

\item{point_alpha}{A numeric value specifying the transparency level of the points. Default is 0.6.}

\item{x_label}{A string specifying the label for the x-axis. Default is \code{NULL} (uses x_var).}

\item{y_label}{A string specifying the label for the y-axis. Default is \code{NULL} (uses y_var or transformed version).}

\item{plot_title}{A string specifying the title of the plot. Default is \code{NULL} (no title).}

\item{verbose}{A boolean indicating whether to print messages about the saved plot locations. Default is TRUE.}
}
\value{
Invisibly returns the generated ggplot object.
}
\description{
This function generates a scatter plot designed for mystery caller studies, allowing for the visualization of waiting times or similar outcomes across different categories, such as insurance types. The function supports transformations on the y-axis, custom jitter, and colors each category in the x-axis using the \code{viridis} color palette. The plot is automatically displayed and saved with a specified resolution.
}
\examples{
\donttest{
example_data <- data.frame(
  insurance = rep(c("Medicaid", "Commercial", "Medicare"), each = 3),
  business_days_until_appointment = c(1.5, 2.0, 2.8, 1.9, 2.4, 2.6, 2.1, 2.7, 3.0)
)

create_scatter_plot(
  plot_data = example_data,
  x_var = "insurance",
  y_var = "business_days_until_appointment",
  y_transform = "none",
  dpi = 50,
  output_dir = tempdir(),
  file_prefix = "demo_scatter",
  x_label = "Insurance",
  y_label = "Waiting Times in Days",
  plot_title = "Example Scatter Plot",
  verbose = FALSE
)
}
}
\seealso{
Other mapping: 
\code{\link{create_density_plot}()},
\code{\link{create_individual_isochrone_plots}()},
\code{\link{create_isochrones}()},
\code{\link{create_isochrones_for_dataframe}()},
\code{\link{create_line_plot}()},
\code{\link{map_create_acog_districts_sf}()},
\code{\link{map_create_base}()},
\code{\link{map_create_block_group_overlap}()},
\code{\link{map_create_leaflet_base}()},
\code{\link{map_create_physician_dot}()}
}
\concept{mapping}
