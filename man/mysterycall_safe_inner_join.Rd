% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_safety.R
\name{mysterycall_safe_inner_join}
\alias{mysterycall_safe_inner_join}
\title{Safe inner join with cardinality checking}
\usage{
mysterycall_safe_inner_join(
  left,
  right,
  by,
  expect_unique_both = TRUE,
  label_left = "left",
  label_right = "right",
  min_coverage = NULL,
  max_duplication = NULL,
  suffix = c(".x", ".y"),
  write_report = FALSE,
  report_prefix = "join_inner"
)
}
\arguments{
\item{left}{A data frame. All rows are preserved.}

\item{right}{A data frame. The lookup / enrichment table.}

\item{by}{Character vector or \code{\link[dplyr:join_by]{dplyr::join_by()}} specifying join keys.
Required; use named vector for cross-column mapping
(\code{c("npi" = "provider_npi")}).}

\item{expect_unique_both}{Logical. When \code{TRUE} (default) keys must be
unique on \strong{both} sides.}

\item{label_left, label_right}{Character scalars used in messages and
the audit report.}

\item{min_coverage}{Numeric in \verb{[0, 1]}. Default \code{0.90}.}

\item{max_duplication}{Numeric >= 0. Maximum allowed output/input row ratio.
Default \code{1.02} (reads \code{JOIN_MAX_DUPLICATION} env var).}

\item{suffix}{Character vector of length 2. Suffixes for conflicting column
names. Default \code{c(".x", ".y")}.}

\item{write_report}{Logical. Write a one-row CSV audit record. Default
\code{FALSE}.}

\item{report_prefix}{Character scalar. Filename prefix for the audit CSV.}
}
\value{
A data frame: the inner-join result.
}
\description{
Wraps \code{\link[dplyr:mutate-joins]{dplyr::inner_join()}} with key-type harmonisation, optional
uniqueness assertion on both tables, coverage enforcement, and an optional
CSV audit record.
}
\details{
Coverage is measured as \code{nrow(semi_join(left, right)) / nrow(left)} to
avoid double-counting from many-to-many multiplication.
}
\examples{
physicians <- data.frame(npi = c("1", "2", "3"), specialty = c("OB", "GYN", "RE"),
                         stringsAsFactors = FALSE)
claims     <- data.frame(npi = c("1", "2"), year = c(2020L, 2020L),
                         stringsAsFactors = FALSE)

result <- mysterycall_safe_inner_join(
  left         = physicians,
  right        = claims,
  by           = "npi",
  label_left   = "physicians",
  label_right  = "claims",
  min_coverage = 0.50,
  write_report = FALSE
)

}
\seealso{
\code{\link[=mysterycall_safe_left_join]{mysterycall_safe_left_join()}}, \code{\link[=mysterycall_safe_semi_join]{mysterycall_safe_semi_join()}},
\code{\link[=mysterycall_safe_anti_join]{mysterycall_safe_anti_join()}},
\code{\link[=mysterycall_assert_unique_keys]{mysterycall_assert_unique_keys()}}

Other safe-joins: 
\code{\link{mysterycall_assert_unique_keys}()},
\code{\link{mysterycall_safe_anti_join}()},
\code{\link{mysterycall_safe_left_join}()},
\code{\link{mysterycall_safe_semi_join}()}
}
\concept{safe-joins}
