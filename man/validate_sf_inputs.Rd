% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geospatial_validators.R
\name{validate_sf_inputs}
\alias{validate_sf_inputs}
\title{Internal helper to validate and prepare sf inputs for spatial workflows.}
\usage{
validate_sf_inputs(
  ...,
  expected_types = NULL,
  auto_fix = TRUE,
  target_crs = NULL,
  context = "geospatial operation"
)
}
\arguments{
\item{...}{Named sf objects to validate.}

\item{expected_types}{Named list mapping object names to allowed geometry
types. A character vector applies to all inputs when names are omitted.}

\item{auto_fix}{Logical indicating whether invalid geometries should be
repaired automatically. Defaults to \code{TRUE}.}

\item{target_crs}{Optional CRS (numeric EPSG code, proj4string, or \code{sf::crs}
object) that all objects should be transformed into before bounding box
checks. When \code{NULL}, the CRS of the first object is used.}

\item{context}{Character string identifying the calling context for clearer
error messages.}
}
\value{
A named list of validated sf objects.
}
\description{
This function ensures objects share a common CRS, have valid geometries,
contain the expected geometry types, possess sensible bounding boxes, and
do not contain empty geometries. Invalid geometries are repaired with
\code{sf::st_make_valid()} when possible.
}
\keyword{internal}
