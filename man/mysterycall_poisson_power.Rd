% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_analysis.R
\name{mysterycall_poisson_power}
\alias{mysterycall_poisson_power}
\title{Sample-size calculation for a Poisson mystery caller study}
\usage{
mysterycall_poisson_power(
  irr,
  lambda_ref,
  alpha = 0.05,
  power = 0.8,
  both_arms = TRUE,
  icc = 0,
  calls_per_cluster = 2L
)
}
\arguments{
\item{irr}{Numeric. The minimum detectable incidence rate ratio. Must be
positive and not equal to 1.}

\item{lambda_ref}{Numeric. Expected mean wait time (days) for the reference
insurance group (e.g. BCBS). Must be positive.}

\item{alpha}{Numeric. Two-sided type I error rate. Default \code{0.05}.}

\item{power}{Numeric. Desired statistical power (1 - beta). Default \code{0.80}.}

\item{both_arms}{Logical. When \code{TRUE}, each provider is called with \strong{both}
insurance types (paired design), halving the total providers needed.
Default \code{TRUE}.}

\item{icc}{Numeric in [0, 1). Intra-cluster correlation if providers are
clustered (e.g. same practice). Applied as a design-effect inflation:
\code{n * (1 + (m - 1) * icc)} where \code{m} is \code{calls_per_cluster}. Default \code{0}
(no clustering adjustment).}

\item{calls_per_cluster}{Integer. Average calls per cluster, used only when
\code{icc > 0}. Default \code{2L}.}
}
\value{
A named list with elements:
\describe{
\item{\code{n_per_arm}}{Providers needed per insurance arm.}
\item{\code{n_total}}{Total providers needed.}
\item{\code{n_total_calls}}{Total calls placed (= \code{n_per_arm * 2} or
\code{n_total * 2} for paired design).}
\item{\code{irr}, \code{lambda_ref}, \code{lambda_trt}}{Input parameters plus derived
treatment-arm mean.}
\item{\code{alpha}, \code{power}}{Input parameters.}
\item{\code{design_effect}}{Inflation factor when \code{icc > 0}.}
}
}
\description{
Computes the number of providers per insurance arm needed to detect a
specified Incidence Rate Ratio (IRR) in a mystery caller study with a
Poisson count outcome (e.g. business days until appointment). Each provider
is called once per insurance arm.
}
\details{
The sample-size formula is derived from the score test for comparing two
independent Poisson rates \eqn{\lambda_0} (reference arm, e.g. BCBS) and
\eqn{\lambda_1 = \text{IRR} \times \lambda_0} (treatment arm, e.g. Medicaid):

\deqn{n = \frac{(z_{\alpha/2} + z_\beta)^2 \left(\frac{1}{\lambda_0} + \frac{1}{\lambda_1}\right)}{(\log \text{IRR})^2}}

This gives providers per arm. Total providers = \code{n * 2}. When each provider
is called with both insurance types (\code{both_arms = TRUE}), only \code{n} providers
are needed in total.
}
\examples{
# Detect IRR = 1.40 (Medicaid 40\% longer waits) vs BCBS mean of 14 days
mysterycall_poisson_power(irr = 1.40, lambda_ref = 14)

# Higher power
mysterycall_poisson_power(irr = 1.40, lambda_ref = 14, power = 0.90)

# Smaller effect (IRR = 1.20)
mysterycall_poisson_power(irr = 1.20, lambda_ref = 14)
}
\seealso{
\code{\link[=mysterycall_cochran_n]{mysterycall_cochran_n()}}, \code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}

Other power analysis: 
\code{\link{mysterycall_cochran_n}()}
}
\concept{power analysis}
