% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_phone.R
\name{mysterycall_validate_phone}
\alias{mysterycall_validate_phone}
\title{Validate North-American (NANP) phone number strings}
\usage{
mysterycall_validate_phone(phone_str, practice_state = NULL, nanp_path = NULL)
}
\arguments{
\item{phone_str}{Character vector of phone strings (NAs allowed).
Accepts common formats: \code{"(NPA) NXX-XXXX"}, \code{"NPA-NXX-XXXX"},
bare 10 digits, or \code{"1NPANXXXXXX"}.}

\item{practice_state}{Character vector of 2-letter US state postal codes,
the same length as \code{phone_str} or length 1 (recycled).  Pass \code{NULL} to
skip the state-matching check (all \code{phone_area_code_matches_state} entries
will be \code{NA}).}

\item{nanp_path}{Optional path to a custom NANP lookup CSV.  When \code{NULL}
(default) the bundled \code{inst/extdata/nanp_area_codes_us.csv} is used.}
}
\value{
A data frame with one row per element of \code{phone_str} and columns:
\describe{
\item{\code{phone_e164_valid}}{Logical. \code{TRUE} if the number passes all NANP
syntactic rules.}
\item{\code{phone_npa}}{Character. The 3-digit area code, or \code{NA} when the
number is syntactically invalid.}
\item{\code{phone_state_from_npa}}{Character. The 2-letter state code for the
area code, or \code{NA} when the area code is not in the lookup.}
\item{\code{phone_area_code_matches_state}}{Logical. \code{TRUE} when
\code{phone_state_from_npa} equals \code{practice_state}; \code{NA} when
\code{practice_state} is \code{NULL}.}
\item{\code{phone_validity_flag}}{Character. One of \code{"valid"},
\code{"missing"}, \code{"invalid_format"}, \code{"unknown_area_code"}, or
\code{"area_code_state_mismatch"}.}
}
}
\description{
Strips non-digit characters, applies North American Numbering Plan (NANP)
syntactic rules, looks up each area code's home state, and optionally
checks whether that state matches the physician's practice state.
}
\details{
Syntactic validity rules applied:
\itemize{
\item Exactly 10 digits after stripping non-digits (an optional leading
\code{"1"} country code is dropped first).
\item NPA first digit (N) is 2–9.
\item NXX first digit (N) is 2–9.
\item NXX is not an N11 code (e.g. 911, 411, 211).
}

Area-code-to-state mapping is loaded from the bundled
\code{inst/extdata/nanp_area_codes_us.csv} file.  Pass \code{nanp_path} to override
with a custom CSV that has columns \code{area_code} (integer) and \code{state}
(2-letter US postal abbreviation).
}
\section{Data format}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{mysterycall_validate_phone(
  c("(303) 555-1212", "555-1212", NA, "800-555-0199"),
  practice_state = "CO"
)
}\if{html}{\out{</div>}}
}

\examples{
mysterycall_validate_phone(
  c("(303) 555-1212", "555-1212", NA),
  practice_state = "CO"
)

# Without state matching
mysterycall_validate_phone(c("2125551234", "0005551234"), practice_state = NULL)
}
\seealso{
\code{\link[=mysterycall_parse_physician_name]{mysterycall_parse_physician_name()}} to structure name strings;
\code{\link[=mysterycall_validate_parsed_names]{mysterycall_validate_parsed_names()}} for name-level quality checks;
\code{\link[=mysterycall_safe_left_join]{mysterycall_safe_left_join()}} to attach validated phone data to a roster.
}
\concept{data quality}
