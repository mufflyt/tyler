% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-verify.R
\name{mysterycall_verify_artifact}
\alias{mysterycall_verify_artifact}
\title{Verify the content-addressable identity of an audit trail JSON file}
\usage{
mysterycall_verify_artifact(audit_path)
}
\arguments{
\item{audit_path}{Character scalar. Path to an \code{audit_trail_*.json} file.}
}
\value{
Invisibly returns \code{TRUE} if verification passes. Throws an
informative error if verification fails or the file cannot be parsed.
}
\description{
Loads an audit JSON written by \code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}}, strips the
volatile fields, recomputes the SHA-256 digest of the stable payload, and
compares it against the stored \code{artifact_id}. A mismatch means the file was
modified after it was written or the \code{artifact_id} was computed with
different logic.
}
\section{Contract}{

\strong{Inputs:}
\itemize{
\item \code{audit_path} must point to a readable JSON file produced by
\code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}} at schema version >= 1.2.0.
}

\strong{Guarantees:}
\itemize{
\item Returns \code{TRUE} (invisibly) only when \code{artifact_id} matches the recomputed
digest. Any other outcome raises an error with a diagnostic message.
\item Never modifies the audit file.
}

\strong{Fails if:}
\itemize{
\item \code{audit_path} does not exist or is not valid JSON.
\item The file was produced by a schema version prior to 1.2.0 (no \code{artifact_id}).
\item The recomputed digest does not match the stored \code{artifact_id}.
}
}

\section{Called By}{

\itemize{
\item Downstream validation scripts and CI pipelines
}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Verify an audit file written by clean_phase1
mysterycall_verify_artifact("path/to/audit_trail_2026-05-09.json")
\dontshow{\}) # examplesIf}
}
\seealso{
Other workflow: 
\code{\link{mysterycall_call_productivity}()},
\code{\link{mysterycall_clean_phase1}()},
\code{\link{mysterycall_clean_phase2}()},
\code{\link{mysterycall_print_dashboard}()},
\code{\link{mysterycall_run_workflow}()},
\code{\link{mysterycall_run_workflow_logged}()},
\code{\link{mysterycall_split_and_save}()}
}
\concept{workflow}
