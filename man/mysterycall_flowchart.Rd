% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flowchart.R
\name{mysterycall_flowchart}
\alias{mysterycall_flowchart}
\title{CONSORT-style inclusion/exclusion flowchart}
\usage{
mysterycall_flowchart(
  steps,
  exclusions = NULL,
  title = NULL,
  node_width = 3.5,
  font_size = 10L
)
}
\arguments{
\item{steps}{Named character vector (or named list coercible to character).
Each name is a step label and each value is the formatted count to display.
Steps are rendered top-to-bottom in the order supplied. Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{c(
  "Physicians in AAOS Directory" = "671",
  "Functioning Phone Number"     = "501",
  "Successfully Called"          = "432"
)
}\if{html}{\out{</div>}}}

\item{exclusions}{Optional named character vector. Names must match names in
\code{steps} (the step \emph{after} which the exclusion occurred); values are the
exclusion description displayed in a side box. Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{c("Functioning Phone Number" = "170 excluded: no functioning phone")
}\if{html}{\out{</div>}}}

\item{title}{Optional character scalar placed above the diagram. \code{NULL}
(default) adds no title node.}

\item{node_width}{Numeric. Fixed width of each flow box in inches. Default \code{3.5}.}

\item{font_size}{Integer. Base font size for node labels. Default \code{10L}.}
}
\value{
A \code{DiagrammeR} / \code{htmlwidget} object. Print it to view in the
RStudio Viewer or a browser; use \code{DiagrammeRsvg::export_svg()} +
\code{rsvg::rsvg_pdf()} to save to PDF.
}
\description{
Produces a top-to-bottom DiagrammeR DOT flow diagram showing the sequential
inclusion and exclusion of providers from the analysis. Each step is a
labelled box with a count; optional exclusion side-boxes branch off to the
right. Requires the \code{DiagrammeR} package (listed in Suggests).
}
\examples{
\dontshow{if (requireNamespace("DiagrammeR", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mysterycall_flowchart(
  steps = c(
    "Physicians in Directory" = "671",
    "Functioning Phone"       = "501",
    "Both Insurance Types"    = "432",
    "Final Analysis"          = "389"
  ),
  exclusions = c(
    "Functioning Phone"    = "170 excluded: no functioning phone",
    "Both Insurance Types" = "69 excluded: Medicaid not accepted",
    "Final Analysis"       = "43 excluded: incomplete call data"
  )
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}
}
\concept{visualization}
