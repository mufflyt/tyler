% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_safety.R
\name{mysterycall_assert_unique_keys}
\alias{mysterycall_assert_unique_keys}
\title{Assert that join key columns are unique}
\usage{
mysterycall_assert_unique_keys(
  .data,
  key_cols,
  label = "table",
  dedupe = FALSE
)
}
\arguments{
\item{.data}{A data frame to check.}

\item{key_cols}{Character vector of column names that together form the key.}

\item{label}{Character scalar used in error/message text. Default \code{"table"}.}

\item{dedupe}{Logical. When \code{TRUE} duplicates are silently removed (first
row per key is kept) and the deduplicated data frame is returned invisibly.
When \code{FALSE} (default) any duplicates raise an error with sample duplicate
values to help locate the problem.}
}
\value{
\code{.data} invisibly (possibly deduplicated). Errors when duplicates
exist and \code{dedupe = FALSE}.
}
\description{
Checks that the specified key columns form a unique key in \code{.data}.
When duplicates are found, either errors with sample duplicate values or
silently deduplicates (keeping the first row per key), depending on
\code{dedupe}.
}
\examples{
df <- data.frame(npi = c("A", "B", "A"), value = 1:3)

# Deduplicate silently
clean <- mysterycall_assert_unique_keys(df, "npi", dedupe = TRUE)
nrow(clean)  # 2

}
\seealso{
Other safe-joins: 
\code{\link{mysterycall_safe_anti_join}()},
\code{\link{mysterycall_safe_inner_join}()},
\code{\link{mysterycall_safe_left_join}()},
\code{\link{mysterycall_safe_semi_join}()}
}
\concept{safe-joins}
