# Validate Google Maps API key

Validate Google Maps API key

## Usage

``` r
mysterycall_validate_google_api(api_key)
```

## Arguments

- api_key:

  Google Maps API key

## Value

List with valid (logical) and error (character) fields
